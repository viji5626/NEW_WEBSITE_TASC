{
  "entities": {
    "ProfileDownload": {
      "title": "ProfileDownload",
      "description": "Log of user downloading the profile",
      "type": "object",
      "properties": {
        "email": {
          "type": "string"
        },
        "displayName": {
          "type": "string"
        },
        "uid": {
          "type": "string"
        },
        "downloadedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["email", "uid"]
    },
    "VisitorCount": {
      "title": "VisitorCount",
      "description": "Store the global visitor counter",
      "type": "object",
      "properties": {
        "count": {
          "type": "integer"
        }
      },
      "required": ["count"]
    }
  },
  "firestore": {
    "profile_downloads/{downloadId}": {
      "schema": "ProfileDownload",
      "description": "Store all profile downloads"
    },
    "site_stats/{statsId}": {
      "schema": "VisitorCount",
      "description": "Store site visitor stats"
    }
  }
}
